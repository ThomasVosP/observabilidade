# Observabilidade — Grupo Servopa
# Fase 1 (dashboard HTML): apenas stdlib, sem dependências externas
# Fase 2 (pipelines de dados): descomentar conforme necessidade

# Extração de dados (fase 2)
requests>=2.31.0
python-dotenv>=1.0.0

# Processamento de planilhas Excel (fase 2)
pandas>=2.1.0
openpyxl>=3.1.2

# Banco de dados (fase 2 — escolher um)
# supabase>=2.3.0
# google-cloud-bigquery>=3.13.0
